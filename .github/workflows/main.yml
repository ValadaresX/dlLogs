name: Find and Replace url
on:
  push:
    branches:
      - main

jobs:
  exemplo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3 
      - name: My Secret
        run: echo ${{ secrets.URL_MASK }}
          # URL_TO_REPLACE: ${{ secrets.URL_TO_REPLACE }}
          # git config --global user.email "brenovaladares27@gmail.com"
          # git config --global user.name "ValadaresX"      
          # git add .
          # git commit -m "Replaced URLs in .py files"
          # git push
   
        


        



        
                     
      
