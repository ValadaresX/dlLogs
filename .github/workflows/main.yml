name: Find and Replace url
on:
  push:
    branches:
      - main

jobs:
  exemplo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
        env:
          URL_MASK: ${{ secrets.URL_MASK }}scripts/copy_logs.py
                     
      
